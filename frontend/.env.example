# Public vars (exposed to browser code via `import.meta.env.PUBLIC_*`)
#
# API base URL (origin only, without trailing slash).
# - Same-origin mode (recommended with Nginx reverse proxy): leave empty, browser calls `/api/...`.
# - Cross-origin mode: set to `https://api.example.com` so browser calls `https://api.example.com/api/...`.
PUBLIC_API_BASE_URL=

# WhatsApp configuration (optional)
# `PUBLIC_WHATSAPP_NUMBER`: E.164-ish number (digits only is fine, example: 51999999999).
PUBLIC_WHATSAPP_NUMBER=
#
# Templates support placeholders:
# - Product template: `{productName}`, `{sku}`
# - Order template: `{orderId}`, `{total}`
PUBLIC_WHATSAPP_TEMPLATE=Hola, quiero consultar por {productName}. SKU: {sku}.
PUBLIC_WHATSAPP_ORDER_TEMPLATE=Hola, quiero consultar por mi pedido #{orderId}. Total: {total}.

# Server-only vars (NOT exposed to the browser; available in SSR/server code only)
# API_INTERNAL_BASE_URL=http://backend:3000

# Runtime (Docker / production)
# PORT=4321
# HOST=0.0.0.0
