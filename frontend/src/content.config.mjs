// This project doesn't currently use Astro content collections.
// We keep an explicit (empty) config file so the generated `.astro/content.d.ts`
// can import it without TypeScript errors in editors.

export const collections = {};

